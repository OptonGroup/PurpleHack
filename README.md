# 📊 Оптимизатор календарного плана проекта

## 🌟 Описание решения

Данное решение предназначено для оптимизации календарного плана проекта с учетом ограничений по времени и ресурсам. Решение использует методы машинного обучения (обучение с подкреплением) для минимизации длительности проекта и оптимизации ресурсных затрат.

### ✨ Особенности решения:

1. **🧠 Использование обучения с подкреплением (Reinforcement Learning)** - для нахождения оптимального расписания используется алгоритм Deep Q-Learning
2. **⚖️ Оптимизация по нескольким параметрам** - длительность проекта, ресурсные затраты и стоимость
3. **🎛️ Возможность настройки весов параметров оптимизации** - можно задать веса для каждого параметра оптимизации
4. **🔗 Учет сложных ограничений** - календари, роли, зависимости между задачами
5. **🌐 Веб-интерфейс** - удобный пользовательский интерфейс для загрузки, оптимизации и просмотра результатов

## 🏗️ Архитектура решения

Решение состоит из следующих компонентов:

1. **📋 Модели данных** - классы для представления задач, ресурсов, зависимостей и календарей
2. **🎮 Среда обучения с подкреплением** - моделирует процесс построения календарного плана
3. **🤖 Агент обучения с подкреплением** - принимает решения о порядке выполнения задач и назначении ресурсов
4. **⚙️ Оптимизатор** - объединяет все компоненты системы для оптимизации календарного плана
5. **🖥️ Веб-приложение** - предоставляет API и пользовательский интерфейс для работы с оптимизатором

## 📋 Требования

- Python 3.10 или выше
- PyTorch (опционально, есть режим демо без PyTorch)
- Pandas
- NumPy
- FastAPI
- Uvicorn
- Jinja2
- Pydantic

## 🔧 Установка

### Установка с помощью pip

```bash
pip install -r requirements.txt
```

### 🐳 Установка с помощью Docker

```bash
# Сборка образа
docker build -t project-optimizer .

# Запуск контейнера
docker run -p 8000:8000 project-optimizer
```

### 🐙 Установка с помощью Docker Compose

```bash
docker-compose up -d
```

## 🚀 Использование

### 🌐 Запуск веб-приложения

```bash
cd web_app
python app.py
```

После запуска веб-приложение будет доступно по адресу: http://localhost:8000

### 💻 Запуск из командной строки

```bash
python main.py --input dataset.json --output optimized_schedule.json --duration-weight 7 --resource-weight 3 --cost-weight 1
```

### ⚙️ Параметры командной строки

- `-i, --input` - путь к входному файлу JSON (по умолчанию: dataset.json)
- `-o, --output` - путь для сохранения оптимизированного плана (по умолчанию: optimized_schedule.json)
- `-d, --duration-weight` - вес длительности проекта при расчете награды (по умолчанию: 1.0)
- `-r, --resource-weight` - вес количества ресурсов при расчете награды (по умолчанию: 1.0)
- `-c, --cost-weight` - вес стоимости проекта при расчете награды (по умолчанию: 1.0)
- `-e, --episodes` - количество эпизодов для обучения (по умолчанию: 100)
- `-m, --model` - путь к сохраненной модели (по умолчанию: None)
- `--save-model` - сохранять ли модель после обучения (по умолчанию: True)
- `--model-save-path` - путь для сохранения модели (по умолчанию: model.pt)
- `--verbose` - выводить детальную информацию о процессе оптимизации

### 📝 Программное использование

```python
from src.optimizer import optimize_schedule

optimized_schedule = optimize_schedule(
    input_file="dataset.json",
    output_file="optimized_schedule.json",
    duration_weight=7.0,
    resource_weight=3.0,
    cost_weight=1.0,
    num_episodes=100
)
```

## 📄 Формат входных данных

Входные данные должны быть представлены в формате JSON и содержать следующие разделы:

- `tasks` - массив задач с указанием длительности, зависимостей и ролей
- `resources` - массив исполнителей с указанием ролей и индивидуальных календарей
- `projectCalendar` - календарь проекта (рабочие и нерабочие дни)
- `dependencies` - массив зависимостей между задачами

## 📤 Формат выходных данных

Выходные данные представляются в формате JSON и содержат:

- `tasks` - массив оптимизированных задач с указанием дат начала и окончания, а также назначенных ресурсов
- `totalDuration` - общая длительность проекта в днях
- `resourceUtilization` - процент использования ресурсов
- `totalCost` - общая стоимость проекта

## 📝 Пример использования

```bash
python main.py --duration-weight 7 --resource-weight 3 --cost-weight 1
```

В этом примере оптимизация будет проводиться с весами: длительность - 7, ресурсы - 3, стоимость - 1.

## 🔍 Пример входных данных (JSON)

```json
{
  "project": {
    "id": "project1",
    "name": "Тестовый проект",
    "startDate": "2024-02-01T00:00:00Z"
  },
  "tasks": {
    "rows": [
      {
        "id": "task1",
        "name": "Задача 1",
        "duration": 5,
        "requiredResourceTypes": ["developer"]
      },
      {
        "id": "task2",
        "name": "Задача 2",
        "duration": 3,
        "requiredResourceTypes": ["designer"]
      }
    ]
  },
  "resources": {
    "rows": [
      {
        "id": "resource1",
        "name": "Иван Иванов",
        "type": "developer",
        "cost": 1000
      },
      {
        "id": "resource2",
        "name": "Петр Петров",
        "type": "designer",
        "cost": 800
      }
    ]
  },
  "dependencies": {
    "rows": [
      {
        "id": "dep1",
        "predecessorTaskId": "task1",
        "successorTaskId": "task2"
      }
    ]
  },
  "projectCalendar": {
    "workingDays": [1, 2, 3, 4, 5],
    "holidays": ["2024-02-23"]
  }
}
```

## 📊 Пример выходных данных (JSON)

```json
{
  "tasks": [
    {
      "id": "task1",
      "startDate": "2024-02-01T00:00:00Z",
      "endDate": "2024-02-07T00:00:00Z",
      "assignedResourceId": "resource1"
    },
    {
      "id": "task2",
      "startDate": "2024-02-08T00:00:00Z",
      "endDate": "2024-02-12T00:00:00Z",
      "assignedResourceId": "resource2"
    }
  ],
  "totalDuration": 12,
  "resourceUtilization": 87.5,
  "totalCost": 8400.0
}
```

## 📚 API Справочник

### Основные эндпоинты API

| Эндпоинт | Метод | Описание |
|---|---|---|
| `/api/validate` | POST | Валидация JSON-файла |
| `/api/optimize` | POST | Запуск оптимизации календарного плана |
| `/api/status/{job_id}` | GET | Получение статуса оптимизации |
| `/api/result/{job_id}` | GET | Получение результата оптимизации |
| `/api/download/{job_id}` | GET | Скачивание результата в формате JSON |

## 🤝 Скриншоты

![image](https://github.com/user-attachments/assets/f0df7979-5b23-43c7-adac-635bc5eea196)


## 📜 Лицензия

MIT License
